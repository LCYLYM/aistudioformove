{
  "name": "aistudioformove",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:web": "vite build",
    "preview": "vite preview",
    "electron:dev": "cross-env ELECTRON_DEV=1 electron .",
    "build:electron": "electron-builder --config electron-builder.yml",
    "cap:sync": "cap sync android",
    "cap:build": "cd android && ./gradlew assembleRelease",
    "build:android": "npm run build:web && npm run cap:sync && npm run cap:build",
    "postinstall": "node scripts/copy-esbuild-wasm.cjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "fflate": "^0.8.2",
    "esbuild-wasm": "^0.24.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react-swc": "^3.7.0",
    "cross-env": "^7.0.3",
    "electron": "^32.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
